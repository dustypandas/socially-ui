import { sampleFullEvent } from '../../dummyData.ts';
import type { EventPageData } from '../../types.ts';

// export async function getEventPageData(): Promise<FullEvent> {
//   return sampleFullEvent;
// }
